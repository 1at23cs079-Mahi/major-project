# =============================================================================
# HiReady Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values.

# Supabase Configuration (required for full functionality)
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# =============================================================================
# Supabase Edge Function Secrets
# =============================================================================
# These are NOT set in .env — they are set via the Supabase CLI:
#   supabase secrets set OPENAI_API_KEY=sk-...
#   supabase secrets set CUSTOM_VLM_ENDPOINT=http://your-gpu-server:8000/v1
#   supabase secrets set CUSTOM_VLM_API_KEY=your-api-key
#
# OPENAI_API_KEY          - OpenAI API key for GPT-4o Vision (fallback VLM)
# CUSTOM_VLM_ENDPOINT     - Self-hosted VLM endpoint (vLLM/TGI, OpenAI-compatible)
# CUSTOM_VLM_API_KEY      - API key for the custom VLM endpoint
#
# Resume Analysis (analyze-resume Edge Function):
# RESUME_LLM_ENDPOINT     - Custom LLM endpoint for resume analysis (optional, defaults to OpenAI)
# RESUME_LLM_API_KEY      - API key for the custom resume LLM (optional, falls back to OPENAI_API_KEY)
# RESUME_LLM_MODEL        - Model name (optional, defaults to gpt-4o-mini)
#
# SUPABASE_URL            - Auto-provided by Supabase Edge Functions runtime
# SUPABASE_SERVICE_ROLE_KEY - Auto-provided by Supabase Edge Functions runtime
